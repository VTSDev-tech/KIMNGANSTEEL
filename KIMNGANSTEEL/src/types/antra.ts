export type NavItem = {
  label: string;
  href: string;
};

export type ServiceCard = {
  title: string;
  description: string;
  icon: string;
};

export type ProjectCard = {
  title: string;
  category: string;
  image: string;
};

export type ProcessStep = {
  title: string;
  description: string;
};
